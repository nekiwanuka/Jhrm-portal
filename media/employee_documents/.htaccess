# Block direct web access to employee documents.
# Files should be accessed via permission-checked Django views.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
  Order Allow,Deny
  Deny from all
</IfModule>
